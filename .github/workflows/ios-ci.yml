name: iOS CI

on:
  # ① Auto-run solo al abrir / re-abrir la PR
  pull_request:
    types: [opened, reopened]
    branches: [main]

  # ② Run a petición vía comentario 'check pr'
  issue_comment:
    types: [created]

  # ③ (Opcional) Lanzar manualmente desde la UI
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-15
    timeout-minutes: 45
    permissions:
      contents: write        # para publicar a gh-pages
      issues: write          # crear / editar comentarios
      pull-requests: write

    # ───────────── Filtro de ejecución ─────────────
    if: |
      ( github.event_name == 'pull_request' ) ||
      ( github.event_name == 'issue_comment'  &&
        github.event.issue.pull_request != null &&
        contains(github.event.comment.body, 'check pr') ) ||
      ( github.event_name == 'workflow_dispatch' )

    steps:
    # ───────────── Comentario de arranque ─────────────
    - name: Comment CI started
      uses: actions/github-script@v7
      if: always()
      with:
        script: |
          const marker = '<!-- ios-ci-marker -->'
          const sha    = context.sha.substring(0,7)

          // 🔎 Localiza nº de PR según el tipo de evento
          async function findPR () {
            if (context.eventName === 'pull_request')
              return context.payload.number

            if (context.payload.issue?.pull_request)
              return context.payload.issue.number

            // workflow_run / dispatch / rerun → buscar por SHA
            const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              commit_sha: context.sha
            })
            return prs.length ? prs[0].number : null
          }

          const prNumber = await findPR()
          if (!prNumber) { core.warning('PR not found'); return }

          // Comprueba si ya existe el comentario
          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo:  context.repo.repo,
            issue_number: prNumber
          })
          const existing = comments.find(c => c.body.includes(marker))
          const body = `🚀 **iOS CI** comenzando *build* + *tests*…\n${marker}`

          if (existing) {
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: existing.id,
              body
            })
          } else {
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              issue_number: prNumber,
              body
            })
          }

    # ───────────── Pipeline de build + test ─────────────
    - uses: actions/checkout@v4
      with: { fetch-depth: 0 }

    - uses: maxim-lobanov/setup-xcode@v1
      with: { xcode-version: 'latest-stable' }

    - name: Resolve Swift Packages
      run: >
        xcodebuild -resolvePackageDependencies
        -project GeemuKore.xcodeproj
        -scheme GeemuKore

    - name: Build & Test (iPhone, iOS 18.6)
      id: buildtest
      run: |
        xcodebuild test \
          -project GeemuKore.xcodeproj \
          -scheme GeemuKore \
          -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.6' \
          -parallel-testing-enabled NO \
          -enableCodeCoverage YES \
          -resultBundlePath TestResults/Test.xcresult \
          clean build |
        xcpretty --simple --color
      env: { NSUnbufferedIO: 'YES' }

    - uses: actions/upload-artifact@v4
      if: always()
      with:
        name: TestResults
        path: TestResults/Test.xcresult

    # (Opcional) Generación y publicación de coverage.json
    - name: Extract coverage percentage
      id: coverage
      run: |
        RESULT_PATH='TestResults/Test.xcresult'
        
        # DEBUG: Imprime el JSON completo que se está leyendo
        REPORT=$(xcrun xccov view --report --json "$RESULT_PATH")
        echo "::group::XCCov JSON Report"
        echo "$REPORT"
        echo "::endgroup::"

        # CÁLCULO
        PERCENT=$(echo "$REPORT" | jq '[.targets[].lineCoverage] | add / length * 100')
        
        # DEBUG: Imprime el resultado del cálculo
        echo "DEBUG: Porcentaje calculado por jq: $PERCENT"

        printf '{ "schemaVersion": 1, "label": "coverage", "message": "%0.1f%%", "color": "%s" }\n' \
               "$PERCENT" "$( [ $(echo "$PERCENT > 80" | bc) -eq 1 ] && echo green || echo orange )" \
               > coverage.json

    - name: Publish coverage JSON
      uses: peaceiris/actions-gh-pages@v4
      if: github.ref == 'refs/heads/main' && job.status == 'success'
      with:
        personal_token: ${{ secrets.GITHUB_TOKEN }}
        external_repository: ${{ github.repository }}
        publish_dir: .
        publish_branch: gh-pages
        destination_dir: coverage
        keep_files: true

    # ───────────── Comentario de resultado ─────────────
    - name: Comment CI result
      uses: actions/github-script@v7
      if: always()
      with:
        script: |
          const outcome = '${{ job.status }}'   // success / failure / cancelled
          const sha     = context.sha.substring(0,7)
          const emoji   = outcome === 'success' ? '✅'
                       : outcome === 'failure' ? '❌'
                       : '⚠️'
          const marker  = '<!-- ios-ci-marker -->'

          // misma util ▼
          async function findPR () {
            if (context.eventName === 'pull_request')
              return context.payload.number
            if (context.payload.issue?.pull_request)
              return context.payload.issue.number
            const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              commit_sha: context.sha
            })
            return prs.length ? prs[0].number : null
          }

          const prNumber = await findPR()
          if (!prNumber) { core.warning('PR not found'); return }

          const { data: comments } = await github.rest.issues.listComments({
            owner: context.repo.owner,
            repo:  context.repo.repo,
            issue_number: prNumber
          })
          const existing = comments.find(c => c.body.includes(marker))
          const body = `${emoji} **iOS CI** ${outcome} on \`${sha}\`.\n${marker}`

          if (existing) {
            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              comment_id: existing.id,
              body
            })
          } else {
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo:  context.repo.repo,
              issue_number: prNumber,
              body
            })
          }
